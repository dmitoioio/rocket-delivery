dist/
node_modules/
package-lock.json

# Прототип зберігається as-is — не форматувати
prototype/

# Хуки й налаштування Claude прийшли зі starter-kit і тримаються як є:
# переформатування розвело б їх із джерелом, а це чужий код, не наш
.claude/

# Markdown не форматуємо: Prettier переламує вирівнювання таблиць у docs/,
# а вони читаються людьми частіше, ніж парсяться інструментами
*.md
