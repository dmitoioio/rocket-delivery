# 10 — Дорожня карта

Принцип: **спершу закрити те, що ламає бізнес, потім те, що ламає код.** Красивий застосунок на зламаній фінмоделі — це дорога помилка.

---

## Етап 0 — Розблокувати (зараз)

| # | Задача | Хто | Блокує |
|---|---|---|---|
| 0.1 | Покласти `rocket-delivery-app.html` у `prototype/` | Власник | Аудит коду, перенесення UI |
| 0.2 | Дати доступ до `Volodymyr221/CSTL_NEWS` | Власник | Усе проєктування БД |
| 0.3 | Відповісти на Q4–Q10 у [11-open-questions.md](11-open-questions.md) | Власник | Етап 4 і пілот |
| 0.4 | Зʼясувати платіжного провайдера cstllife | Власник | Рефанди (B3) |

> ~~Без 0.2 будь-яка схема БД — здогадка.~~ **Знято 28.07.2026** рішенням [ADR-0011](adr/0011-separate-delivery-schema.md): наші таблиці живуть в окремій схемі `delivery`, тож колізія з CSTL_NEWS неможлива за побудовою, а не малоймовірна. Доступ 0.2 усе ще потрібен — але для звірки авторизації й підключення, а не для проєктування.

---

## Етап 1 — Правила ⏸️ частково відкладено

**Фінмодель (Q1–Q3) відкладена рішенням власника від 26.07.2026.** Розробка на неї **не чекає**.

Замість затвердження цифр етап зводиться до одного: побудувати схему так, щоб будь-яка модель підключалась конфігом.

| # | Задача | Статус | Закриває |
|---|---|---|---|
| 1.1 | Форма зайнятості курʼєрів і схема оплати | ✅ Вирішено: Glovo-модель | B1, Q1 |
| 1.2 | Тарифікація доставки (зони чи фікс) | ⏸️ Відкладено | Q2 |
| 1.3 | Комісія із «Суші Мар» | ⏸️ Відкладено | Q3 |
| 1.4 | **Hard limit радіусу доставки** | 🔴 Не відкладається | B10 |
| 1.5 | Політика `failed_delivery` | Потрібно до пілоту | B5, Q4 |
| 1.6 | Правило таймауту підтвердження готівки | Потрібно до пілоту | B4, Q5 |
| 1.7 | Процес рефанду й SLA | Потрібно до пілоту | B3, Q6 |
| 1.8 | Ліміт готівки на руках | Потрібно до пілоту | B2 |

**1.4 не відкладається разом із рештою** — це не про гроші, а про фізику: без обмеження радіусу система колись прийме замовлення за 30 км, і курʼєр на скутері туди просто не доїде. Значення можна поставити будь-яке (стартово 15 км) і змінити потім.

**Що робимо натомість, щоб відкладення лишалось дешевим** — реалізується на етапі 3:

- `orders.delivery_fee` і `earnings_log.amount` зберігають суму на момент події, не читають конфіг;
- ставки в конфігурації БД, не в коді;
- схема тримає **і погодинну, і відрядну** оплату (`earnings_log.reason` + порожня таблиця `shifts`).

> ⚠️ Останній пункт — єдине, що відкладення робить дорожчим, якщо його проігнорувати. Форма оплати змінює форму даних: погодинна модель вимагає обліку робочого часу, якого у відрядній немає. Кілька полів зараз проти міграції із зарплатними даними всередині — потім.

---

## Етап 2 — Дослідження CSTL_NEWS

| # | Задача |
|---|---|
| 2.1 | Прочитати `supabase/functions` — що вже є |
| 2.2 | Зафіксувати наявні таблиці й назви, знайти колізії з нашою схемою |
| 2.3 | Розібратись, як влаштована авторизація й де живе роль користувача |
| 2.4 | **Перевірити, чи виправлено витік RLS на приватних повідомленнях** — і чи є ще таблиці без політик |
| 2.5 | Зʼясувати, чи є середовище staging окремо від продакшену |

**Вихід:** ADR про те, як Rocket Delivery вбудовується в наявну схему.

---

## Етап 3 — Схема БД і RLS ✅ виконано

| # | Задача | Закриває |
|---|---|---|
| 3.1 | Міграція: enum-типи + `businesses`, `orders`, `couriers` | — |
| 3.2 | `estimated_ready_at` + `ready_at` як два різні поля | B8 |
| 3.3 | `trip_id` і `max_active_orders` одразу, до батч-UI | B29 |
| 3.4 | `order_status_events` (append-only) | B30 |
| 3.5 | `cash_handoffs`, `earnings_log`, `payrolls`, `cancellations` | — |
| 3.5a | Порожня `shifts` + `earnings_log.reason` під погодинну оплату | Q1 (відкладене) |
| 3.5b | Ставки в таблиці конфігурації, не в коді | Q1–Q3 (відкладені) |
| 3.6 | `courier_locations` як «остання позиція» + окремий трек-лог із TTL | B16 |
| 3.7 | RLS на кожній таблиці + тести доступу для 4 ролей і аноніма | B14 |
| 3.8 | VIEW `courier_queue` без приватних колонок + RPC `get_order_contact()` | B15 |
| 3.9 | Тригери-інваріанти (фото перед `delivered`, ліміт активних, `earnings_log`) | — |
| 3.10 | Приватний Storage-бакет для фото + TTL 90 днів | B13 |

**Правило етапу:** жодна міграція не мержиться без RLS-політики й тесту доступу в тому ж PR. Дотримано: `supabase/migrations/` + `supabase/tests/` в одному коміті.

Лишається 3.10 — приватний бакет Storage для фото з TTL 90 днів (B13): потребує реального проєкту Supabase.

---

## Етап 4 — Серверна логіка ✅ здебільшого виконано

| # | Задача | Закриває |
|---|---|---|
| 4.1 | 🔴 Атомарне взяття замовлення (умовний `UPDATE`) — **основна механіка** | B25 |
| 4.1a | `waiting_bonus`: прогресивна надбавка за час у черзі | B35 |
| 4.1b | Перевірка доступності курʼєрів на етапі оформлення | B36 |
| 4.1c | Знижений ліміт готівки для новачків + утримання з заробітку | B38 |
| 4.2 | `idempotency_key` на всіх діях зміни стану | B24 |
| 4.3 | Edge Function створення курʼєра (`service_role`, разовий показ пароля) | B17 |
| 4.4 | Watchdog за розкладом + алерти | B7 |
| 4.5 | Лічильник повернень і ескалація | B6 |
| 4.6 | Розрахунок відстані й перевірка радіусу на сервері | B10 |
| 4.7 | Процес рефанду зі статусами | B3 |

Виконано 4.1–4.6 у `supabase/migrations/0002_functions.sql` і `0004_auth_realtime.sql`.
**4.7 заблоковано** — платіжний провайдер cstllife невідомий (Q6). Прапорець `refund_needed` ставиться, сам процес повернення не написаний.

---

## Етап 5 — Застосунок ✅ здебільшого готово

Побудований на **підмінному адаптері даних**: уся бізнес-логіка й UI працюють уже зараз на моковому бекенді, підключення Supabase — заміна одного модуля, а не переписування екранів.

| # | Задача | Стан | Закриває |
|---|---|---|---|
| 5.1 | Модульна структура `src/` замість одного файлу | ✅ | — |
| 5.2 | Курʼєрський флоу: черга → взяти → степер → фото → PIN → готівка | ✅ | — |
| 5.3 | Адмінка: 7 екранів | ✅ | — |
| 5.4 | Атомарне взяття + UX програшу гонки | ✅ | B25 |
| 5.5 | Черга без способу оплати й приватних даних | ✅ | B15, B35 |
| 5.6 | Надбавка за очікування, видима в черзі | ✅ | B35 |
| 5.7 | Офлайн-черга дій в IndexedDB + ідемпотентність | ✅ | B23, B24 |
| 5.8 | Геолокація + Wake Lock із перезапитом після фону | ✅ | — |
| 5.9 | Обробка відмови в дозволі на геолокацію | ✅ | B22 |
| 5.10 | `delivery_pin` з керованим обходом | ✅ | B12 |
| 5.11 | Ліміт готівки як прогрес, знижений для новачків | ✅ | B2, B38 |
| 5.12 | Демо-доступи поза продакшн-бандлом + перевірка в CI | ✅ | B18 |
| 5.13 | Свайп ігнорується всередині карти | ✅ | B27 |
| 5.14 | Екранування всіх даних від людей (XSS) | ✅ | — |
| 5.15 | Правила з паперу в код: радіус, онлайн-гейт, графік, watchdog | ✅ | B6, B7, B10, B34 |
| 5.16 | Гроші сходяться: здача ↔ замовлення, борг, відомість, виплата | ✅ | B2, B4 |
| 5.17 | Поверхні cstllife як симуляція в демо (клієнт, заклад) | ✅ | B8, B41 |
| 5.18 | Позиція курʼєра доходить до адміна; карта зі справжньою геометрією | ✅ | B16, B20 |
| 5.19 | Фото як доказ, який видно; журнал із імʼям виконавця | ✅ | B13 |
| 5.20 | Пульт демо: генератор замовлень, автопілот кухні, графік | ✅ | — |
| 5.21 | Схема БД, RLS і серверні функції — 60 перевірок проти Postgres | ✅ | B7, B8, B15, B16, B24, B25, B30, B41 |
| 5.22 | Продакшн-адаптер: 20 заглушок → робочий код | ✅ | — |
| 5.23 | Авторизація, Edge Function створення курʼєра | ✅ | B17 |
| 5.24 | Realtime-підписки з опитуванням як страховкою | ✅ | — |
| 5.25 | Контракт інтеграції з cstllife ([docs/14](14-cstllife-integration.md)) | ✅ | — |
| 5.26 | Тайли карти зі своїм провайдером | ⬜ | B20 |
| 5.27 | Web Push для встановленого PWA | ⬜ | B32 |

Лишились 5.26–5.27: тайли потребують ключа провайдера, push — окрема робота.

⚠️ **Бекенд написаний і перевірений, але ще не підключений.** Потрібні три речі, і всі три поза кодом: проєкт Supabase (URL + ключі), ідентифікатор картки бізнесу в cstllife, і піндроп координат у їхньому чекауті. Деталі — [14-cstllife-integration.md](14-cstllife-integration.md).

**Перевірено:** 69 юніт-тестів бізнес-правил і геометрії, 45 браузерних перевірок, наскрізний сценарій із 17 ланок (клієнт оформлює → заклад готує → курʼєр везе й звіряє код → адмін виплачує), 10 перевірок локації, доказів і журналу, 11 перевірок пульта. Плюс перевірка в CI, що жодна демо-поверхня не потрапляє в продакшн-бандл.

✅ **Перенесення в базу виконано.** Мок лишається виконуваною специфікацією й еталоном для порівняння, але правила тепер живуть у Postgres: радіус, гонка, звірка коду, нарахування грошей. Перевірка на клієнті захистом не є — і тепер вона там не єдина.

**Окремо перевірено проти живої бази** (`npm run db:test`, 60 перевірок): курʼєр Б не бачить чужих замовлень, не дістає чужий контакт, не видаляє журнал; «доставлено» без фото відхиляє сама база; два одночасні взяття дають рівно одного переможця.

---

## Етап 6 — Інтеграція з cstllife

Поведінка цих екранів уже описана робочою симуляцією в `src/features/demo/` — `business.js` і `client.js`. Це не реалізація (вона належить cstllife за ADR-0001), але вона знімає з етапу невизначеність: видно, які поля потрібні, які дії існують і що саме бачить кожна сторона.

| # | Задача | Прототип |
|---|---|---|
| 6.1 | Кнопка «готово до забору» в картці бізнесу — реальний тригер `ready_at` замість демо-таймера | `demo/business.js` |
| 6.2 | Чекаут «Суші Мар»: вибір оплати, піндроп адреси на карті, поле орієнтира | `demo/client.js` (без піндропа) |
| 6.3 | Графік роботи закладу — блокування замовлень поза годинами (B34) | ✅ логіка готова |
| 6.4 | Сторінка статусу замовлення клієнта з живою локацією курʼєра | `demo/client.js` (без локації) |
| 6.5 | Локація курʼєра в картці бізнесу | ⬜ |
| 6.6 | Екран підтвердження здачі готівки на боці закладу | `demo/business.js`, вкладка «Готівка» |

---

## Етап 7 — Пілот «Суші Мар»

| # | Задача |
|---|---|
| 7.1 | Seed реальних даних, облікові записи курʼєрів |
| 7.2 | Тримачі із зарядкою для телефонів (B21) — операційна підготовка |
| 7.3 | Інструкція для курʼєра і для закладу (одна сторінка кожна) |
| 7.4 | Тиждень тіньового запуску: справжні замовлення, адмін дублює вручну |
| 7.5 | Заміри за метриками з [06](06-financial-model.md) |
| 7.6 | Рішення про продовження на основі точки беззбитковості (90 замовлень/міс) |

---

## Після пілоту

Порядок визначається даними пілоту, не цим списком.

- Батчинг (`trip_id` уже в схемі)
- Другий заклад — перевірка, що жодного хардкоду «Суші Мар» не лишилось
- Рейтинги й скарги клієнтів (B31)
- Графік змін курʼєрів (B33)
- Платіжна інтеграція замість ручних рефандів
- Нативна обгортка (Capacitor), якщо Wake Lock виявиться недостатнім

---

## Чого в цій карті свідомо немає

**Дат.** Проєкт має одного власника, невизначений обсяг участі розробки і чотири незакриті бізнес-питання. Дати, виставлені зараз, будуть вигадкою і зашкодять більше, ніж їх відсутність.

Дати зʼявляються після етапу 1, коли обсяг перестане плавати.
